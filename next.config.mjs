/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // La configuración de imágenes no la usaremos por ahora, pero la dejamos para futuras necesidades.
  images: {
    domains: [],
  },
};

export default nextConfig;
